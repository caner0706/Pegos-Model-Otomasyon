name: Notebook Otomasyon

on:
  schedule:
    - cron: "0 */2 * * *"   # her 2 saatte bir çalışır
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repo Checkout
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          
      - name: Google Chrome Kur
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip xvfb libxi6 libgbm-dev libnss3 lsb-release xdg-utils fonts-liberation
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt-get -fy install

      - name: Bağımlılıkları Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install jupyter nbconvert pandas

      - name: Notebook Çalıştır
        run: jupyter nbconvert --to notebook --execute "x.ipynb" --output sonuc.ipynb

      # 🆕 2. ADIM: CSV'leri Birleştir
      - name: CSV Birleştirme
        run: python scripts/merge_csv.py

      # Artifact olarak yükle
      - name: Save Merged CSV
        uses: actions/upload-artifact@v4
        with:
          name: merged-csv
          path: Birleştirilmis_CSV/Birleştirilmis_CSV.csv

      - name: Notebook Çıktısını Yükle
        uses: actions/upload-artifact@v4
        with:
          name: notebook-output
          path: sonuc.ipynb

      - name: JSON Çıktıyı Yükle
        uses: actions/upload-artifact@v4
        with:
          name: json-output
          path: sonuc.json
